# Deploy Clarity Polish 1.3

Overlay this package on the current `moyosoresaibu-website` repository and keep the existing Cloudflare `/functions/` folder and Worker configuration.

This release removes no core content. It mainly improves mobile breathing room, removes structural-number density, keeps the GitHub pulse out, and makes the before/after example static and clear on phones.

After Cloudflare deploys, check `/`, `/start/`, `/about`, `/experience`, `/projects`, `/insights/`, `/contact`, and `/evidence` in incognito mode. Test 320px, 375px, 390px and desktop. Then run Lighthouse/PageSpeed on the production URL.
