# Deploy — Solution-First 8.1

Overlay this package on the existing website repository. Keep the existing `/functions/` directory and Cloudflare Worker configuration.

Key upgrade areas:
- full-site broken-link and responsive fixes
- decision psychology section
- customer-first copy
- language-control hardening

After deploy:
1. Open `/`, `/projects`, `/growth-system`, `/retention-system`, `/about`, `/experience`, `/insights/`, `/evidence`, `/contact`, `/start/`.
2. Test WhatsApp and Telegram on a real phone.
3. Submit one controlled contact-form test.
4. Run Lighthouse mobile first, then desktop.
